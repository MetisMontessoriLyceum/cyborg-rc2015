#include "NXCDefs.h"
typedef struct {
  int light;
  int dark;
} CaliSens;

task main () {
  CaliSens array[2];
  array[0].light = 5;
  array[0].dark = 10;
  NumOut(0,LCD_LINE1,array[0].dark);
  
  until(false);
}
